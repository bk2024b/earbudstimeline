import Link from 'next/link';
import { getAllEarbuds, getBrands } from '@/lib/queries';
import { canonicalFor, JsonLd } from '@/lib/seo';
import { Footer } from '@/components/UI';
import AdSlot from '@/components/AdSlot';
import { fmtDate } from '@/lib/format';

export const revalidate = 3600;
const n = (v) => Number.isFinite(Number(v)) ? Number(v) : null;
const price = (v) => n(v) == null ? '—' : `$${Math.round(n(v))}`;
const score = (m) => {
  let s = 45;
  const w = n(m.weight_g); if (w != null && w <= 7) s += 15;
  const b = n(m.battery_bud_h); if (b != null && b >= 6) s += 12;
  if (m.transparency_mode || m.transparency) s += 8;
  if (m.anc) s += 5;
  const ip = String(m.water_resistance ?? m.ip_rating ?? '').toUpperCase();
  if (ip.includes('IPX4') || ip.includes('IPX5') || ip.includes('IPX6') || ip.includes('IPX7') || ip.includes('IPX8')) s += 15;
  return Math.min(100, s);
};
export async function generateMetadata({ params }) {
  const { locale } = params;
  const fr = locale === 'fr';
  const year = new Date().getFullYear();
  const titleBase = fr ? 'Meilleurs écouteurs pour courir' : 'Best Earbuds for Running';

  // Le pick n°1 change selon la page (chaque guide a sa propre fonction de score
  // ci-dessus), donc l'inclure dans la description rend chaque page unique aux yeux
  // de Google au lieu de répéter un texte quasi identique sur les 22 guides
  // "best-earbuds-for-*". Si la requête échoue, on retombe sur la description
  // générique existante — jamais de meta vide ou cassée.
  let topPickLine = '';
  try {
    const models = await getAllEarbuds();
    const top = [...models].sort((a, b) => score(b) - score(a))[0];
    if (top) topPickLine = fr ? ` Top actuel : ${top.name}.` : ` Current top pick: ${top.name}.`;
  } catch {}

  const description =
    (fr
      ? 'Comparez les meilleurs écouteurs pour courir selon résistance à l’eau, poids, autonomie et fonctions disponibles.'
      : 'Compare the best earbuds for running using water resistance, weight, battery life and available features.') + topPickLine;

  return {
    title: `${titleBase} ${fr ? 'en' : 'in'} ${year} | EarbudsTimeline`,
    description,
    ...canonicalFor(`/${locale}/guides/best-earbuds-for-running`),
  };
}
export default async function Page({ params }) {
  const { locale } = params; const fr = locale === 'fr'; const [models, brands] = await Promise.all([getAllEarbuds(), getBrands()]); const bm = new Map(brands.map(b => [b.id, b])); const rows = models.map(model => ({ model, score: score(model) })).sort((a,b) => b.score-a.score); const best = rows.slice(0,10); const budget = rows.filter(r => n(r.model.price) != null && n(r.model.price) <= 100).slice(0,5); const year = new Date().getFullYear();
  const todayIso = new Date().toISOString().slice(0, 10);
  const updatedLabel = fmtDate(todayIso, locale);
  const title = fr ? `Meilleurs écouteurs pour courir en ${year}` : `Best Earbuds for Running in ${year}`; const intro = fr ? 'Une sélection pensée pour la course et les entraînements, basée sur les caractéristiques disponibles dans notre catalogue. Le score est un indicateur de spécifications et ne remplace pas un test réel de maintien.' : 'A selection for running and workouts based on specifications available in our catalog. The score is a specification indicator and does not replace real-world fit testing.'; const jsonLd = {'@context':'https://schema.org','@type':'Article',headline:title,description:intro,url:`https://earbudstimeline.com/${locale}/guides/best-earbuds-for-running`,dateModified: todayIso,inLanguage:locale}; const guide = slug => `/${locale}/guides/${slug}`;
  return <><JsonLd data={jsonLd}/><article className="max-w-6xl mx-auto"><div className="font-mono text-xs text-accent uppercase mb-3">Running · {year}</div><h1 className="font-display font-bold text-[34px] sm:text-[48px] leading-tight mb-4">{title}</h1><p className="text-dim text-[15px] sm:text-[17px] leading-7 max-w-3xl">{intro}</p><div className="mt-4 text-[10px] font-mono text-dim">{fr ? `Dernière mise à jour : ${updatedLabel}` : `Last updated: ${updatedLabel}`}</div><section className="mt-8 grid grid-cols-2 gap-3"><a href="#overall" className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">OVERALL</div><div className="font-display font-semibold mt-1">{best.length} picks</div></a><a href="#budget" className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">UNDER $100</div><div className="font-display font-semibold mt-1">{budget.length} picks</div></a></section><section id="overall" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">🏃 {fr ? 'Meilleurs écouteurs pour courir' : 'Best Earbuds for Running Overall'}</h2><div className="overflow-x-auto border border-line rounded-2xl bg-panel"><table className="w-full text-left text-xs"><thead className="border-b border-line font-mono text-[10px] text-dim uppercase"><tr><th className="p-4">#</th><th className="p-4">{fr ? 'Écouteurs' : 'Earbuds'}</th><th className="p-4">{fr ? 'Prix' : 'Price'}</th><th className="p-4">Score</th><th className="p-4">{fr ? 'Résistance' : 'Resistance'}</th></tr></thead><tbody>{best.map((r,i) => <tr key={r.model.id} className="border-b border-line last:border-0"><td className="p-4 text-accent">{i+1}</td><td className="p-4"><Link href={`/${locale}/ecouteurs/${r.model.id}`} className="font-semibold hover:text-accent">{r.model.name}</Link><div className="text-dim text-[10px]">{bm.get(r.model.brand_id)?.name || r.model.brand_id}</div></td><td className="p-4 font-mono">{price(r.model.price)}</td><td className="p-4 font-bold">{r.score}</td><td className="p-4">{r.model.water_resistance || r.model.ip_rating || '—'}</td></tr>)}</tbody></table></div></section><section id="budget" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">💵 {fr ? 'Meilleurs écouteurs pour courir sous 100 $' : 'Best Running Earbuds Under $100'}</h2><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{budget.map(r => <Link key={r.model.id} href={`/${locale}/ecouteurs/${r.model.id}`} className="bg-panel border border-line rounded-2xl p-5 hover:border-accent"><div className="font-mono text-[10px] text-accent">{bm.get(r.model.brand_id)?.name || r.model.brand_id}</div><h3 className="font-display font-semibold mt-1">{r.model.name}</h3><div className="mt-3 font-display font-bold">{price(r.model.price)}</div><div className="mt-3 text-[10px] font-mono text-dim">{r.model.water_resistance || r.model.ip_rating || 'Resistance data —'} · {n(r.model.weight_g) != null ? `${n(r.model.weight_g)}g` : 'Weight —'}</div></Link>)}</div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Pourquoi la résistance à l’eau compte' : 'Why water resistance matters'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'La transpiration et l’exposition à l’humidité rendent la résistance à l’eau importante pour le sport. Une certification IP peut fournir une indication utile, mais elle ne garantit pas que chaque écouteur conviendra à toutes les conditions d’entraînement.' : 'Sweat and moisture exposure make water resistance important for sports. An IP rating can provide a useful indication, but it does not guarantee that every earbud is suitable for every training condition.'}</p></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Maintien, poids et autonomie' : 'Fit, weight and battery life'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Pour courir, un maintien stable et un poids raisonnable peuvent être plus importants que certaines fonctions premium. Une bonne autonomie évite également les recharges fréquentes entre les séances.' : 'For running, secure fit and reasonable weight can matter more than some premium features. Good battery life also reduces charging between workouts.'}</p></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Méthodologie' : 'Methodology'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Le score Running combine résistance à l’eau lorsqu’elle est disponible, poids, autonomie, transparence et ANC. Les données manquantes ne sont pas inventées et le score ne mesure pas le maintien réel pendant la course.' : 'The Running score combines available water resistance, weight, battery life, transparency and ANC data. Missing data is not invented, and the score does not measure real-world fit during running.'}</p></section><section className="mt-14 grid md:grid-cols-2 gap-4"><div className="border border-line rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr ? 'Guides associés' : 'Related guides'}</h2><div className="mt-4 space-y-2 text-sm"><Link className="block hover:text-accent" href={guide('best-earbuds-for-sport')}>→ Best Earbuds for Sport</Link><Link className="block hover:text-accent" href={guide('best-earbuds-for-working')}>→ Best Earbuds for Working</Link><Link className="block hover:text-accent" href={guide('best-earbuds-for-commuting')}>→ Best Earbuds for Commuting</Link></div></div><div className="border border-accent/30 rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr ? 'Explorer le catalogue' : 'Explore the catalog'}</h2><Link className="inline-block mt-4 text-accent font-mono text-xs uppercase" href={`/${locale}/ecouteurs`}>{fr ? 'Voir le catalogue →' : 'Browse catalog →'}</Link></div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-5">FAQ</h2><div className="divide-y divide-line border-y border-line">{(fr ? [['Quels sont les meilleurs écouteurs pour courir ?','Privilégiez notamment un maintien stable, une résistance adaptée à la transpiration, un poids raisonnable et une autonomie suffisante.'],['Les écouteurs doivent-ils être étanches pour courir ?','Une résistance à l’eau adaptée à l’usage sportif est utile, mais le niveau nécessaire dépend des conditions d’utilisation.'],['Les écouteurs ANC sont-ils adaptés à la course ?','Ils peuvent l’être, mais le mode transparence peut être préférable lorsque vous devez rester conscient de votre environnement.']] : [['What are the best earbuds for running?','Look for secure fit, suitable sweat resistance, reasonable weight and enough battery life.'],['Do running earbuds need to be waterproof?','Water resistance suited to sports use is useful, but the required level depends on your conditions.'],['Are ANC earbuds good for running?','They can be, but transparency mode may be preferable when you need awareness of your surroundings.']]).map(([q,a]) => <details key={q} className="py-4"><summary className="cursor-pointer font-display font-medium">{q}</summary><p className="text-dim text-sm leading-7 mt-3 max-w-3xl">{a}</p></details>)}</div></section></article><AdSlot variant="native" zoneKey={process.env.NEXT_PUBLIC_ADSTERRA_SITEWIDE_NATIVE_KEY} invokeDomain={process.env.NEXT_PUBLIC_ADSTERRA_SITEWIDE_NATIVE_DOMAIN} label={locale === 'en' ? 'Advertisement' : 'Publicité'} /><Footer /></>;
}
