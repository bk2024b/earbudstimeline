import Link from 'next/link';
import { getAllEarbuds, getBrands } from '@/lib/queries';
import { canonicalFor, JsonLd } from '@/lib/seo';
import { Footer } from '@/components/UI';

export const revalidate = 3600;
const n = (v) => Number.isFinite(Number(v)) ? Number(v) : null;
const price = (v) => n(v) == null ? '—' : `$${Math.round(n(v))}`;
const travelScore = (m) => {
  let s = 45;
  if (m.anc) s += 25;
  if (m.transparency_mode || m.transparency) s += 8;
  const b = n(m.battery_bud_h); if (b != null && b >= 6) s += 10;
  const w = n(m.weight_g); if (w != null && w <= 7) s += 7;
  return Math.min(100, s);
};
export async function generateMetadata({ params }) {
  const { locale } = params;
  const title = locale === 'fr' ? 'Meilleurs écouteurs pour voyager en 2026' : 'Best Earbuds for Travel in 2026';
  return { title: `${title} | EarbudsTimeline`, description: locale === 'fr' ? 'Comparez les meilleurs écouteurs pour voyager selon ANC, autonomie, poids et mode transparence.' : 'Compare the best earbuds for travel using ANC, battery life, weight and transparency mode.', ...canonicalFor(`/${locale}/guides/best-earbuds-for-travel`) };
}
export default async function Page({ params }) {
  const { locale } = params; const fr = locale === 'fr';
  const [models, brands] = await Promise.all([getAllEarbuds(), getBrands()]);
  const bm = new Map(brands.map(b => [b.id, b]));
  const rows = models.map(model => ({ model, score: travelScore(model) })).sort((a,b) => b.score-a.score);
  const best = rows.slice(0,10); const budget = rows.filter(r => n(r.model.price) != null && n(r.model.price) <= 100).slice(0,5);
  const title = fr ? 'Meilleurs écouteurs pour voyager en 2026' : 'Best Earbuds for Travel in 2026';
  const intro = fr ? 'Une sélection pour les voyages, vols, trains et déplacements basée sur les caractéristiques disponibles dans notre catalogue.' : 'A selection for travel, flights, trains and trips based on specifications available in our catalog.';
  const jsonLd = {'@context':'https://schema.org','@type':'Article',headline:title,description:intro,url:`https://earbudstimeline.com/${locale}/guides/best-earbuds-for-travel`,dateModified:'2026-08-26',inLanguage:locale};
  const guide = slug => `/${locale}/guides/${slug}`;
  return <><JsonLd data={jsonLd}/><article className="max-w-6xl mx-auto"><div className="font-mono text-xs text-accent uppercase mb-3">Travel · 2026</div><h1 className="font-display font-bold text-[34px] sm:text-[48px] leading-tight mb-4">{title}</h1><p className="text-dim text-[15px] sm:text-[17px] leading-7 max-w-3xl">{intro}</p><div className="mt-4 text-[10px] font-mono text-dim">{fr ? 'Dernière mise à jour : 26 août 2026' : 'Last updated: August 26, 2026'}</div><section className="mt-8 grid grid-cols-2 gap-3"><a href="#overall" className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">OVERALL</div><div className="font-display font-semibold mt-1">{best.length} picks</div></a><a href="#budget" className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">UNDER $100</div><div className="font-display font-semibold mt-1">{budget.length} picks</div></a></section><section id="overall" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">✈️ {fr ? 'Meilleurs écouteurs pour voyager' : 'Best Earbuds for Travel Overall'}</h2><div className="overflow-x-auto border border-line rounded-2xl bg-panel"><table className="w-full text-left text-xs"><thead className="border-b border-line font-mono text-[10px] text-dim uppercase"><tr><th className="p-4">#</th><th className="p-4">{fr ? 'Écouteurs' : 'Earbuds'}</th><th className="p-4">{fr ? 'Prix' : 'Price'}</th><th className="p-4">Score</th><th className="p-4">ANC</th></tr></thead><tbody>{best.map((r,i) => <tr key={r.model.id} className="border-b border-line last:border-0"><td className="p-4 text-accent">{i+1}</td><td className="p-4"><Link href={`/${locale}/ecouteurs/${r.model.id}`} className="font-semibold hover:text-accent">{r.model.name}</Link><div className="text-dim text-[10px]">{bm.get(r.model.brand_id)?.name || r.model.brand_id}</div></td><td className="p-4 font-mono">{price(r.model.price)}</td><td className="p-4 font-bold">{r.score}</td><td className="p-4">{r.model.anc ? 'Yes' : '—'}</td></tr>)}</tbody></table></div></section><section id="budget" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">💵 {fr ? 'Meilleurs écouteurs de voyage sous 100 $' : 'Best Travel Earbuds Under $100'}</h2><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{budget.map(r => <Link key={r.model.id} href={`/${locale}/ecouteurs/${r.model.id}`} className="bg-panel border border-line rounded-2xl p-5 hover:border-accent"><div className="font-mono text-[10px] text-accent">{bm.get(r.model.brand_id)?.name || r.model.brand_id}</div><h3 className="font-display font-semibold mt-1">{r.model.name}</h3><div className="mt-3 font-display font-bold">{price(r.model.price)}</div><div className="mt-3 text-[10px] font-mono text-dim">{r.model.anc ? 'ANC' : 'No ANC'} · {n(r.model.battery_bud_h) != null ? `${n(r.model.battery_bud_h)}h` : 'Battery —'}</div></Link>)}</div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Pourquoi l’ANC compte en voyage' : 'Why ANC matters for travel'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Dans un avion, un train ou un environnement urbain, l’ANC peut aider à réduire certains bruits ambiants continus. Son efficacité varie selon le modèle et le type de bruit.' : 'On planes, trains and in urban environments, ANC can help reduce some continuous ambient sounds. Effectiveness varies by model and noise type.'}</p></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Autonomie pour les longues journées' : 'Battery life for long trips'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Une autonomie élevée peut être pratique pendant les déplacements et limiter les recharges. L’ANC, les appels et le volume peuvent toutefois réduire l’autonomie réelle.' : 'Long battery life can be useful during trips and reduce charging interruptions. ANC, calls and volume can affect real-world runtime.'}</p></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Méthodologie' : 'Methodology'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Le score Voyage combine ANC, mode transparence, autonomie et poids lorsqu’ils sont disponibles. Il s’agit d’un indicateur de spécifications, pas d’un test réel d’isolation ou de confort.' : 'The Travel score combines available ANC, transparency, battery life and weight data. It is a specification indicator, not a real-world isolation or comfort test.'}</p></section><section className="mt-14 grid md:grid-cols-2 gap-4"><div className="border border-line rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr ? 'Guides associés' : 'Related guides'}</h2><div className="mt-4 space-y-2 text-sm"><Link className="block hover:text-accent" href={guide('best-noise-cancelling-earbuds')}>→ Best Noise Cancelling Earbuds</Link><Link className="block hover:text-accent" href={guide('best-earbuds-for-commuting')}>→ Best Earbuds for Commuting</Link><Link className="block hover:text-accent" href={guide('best-battery-life-earbuds')}>→ Best Battery Life Earbuds</Link></div></div><div className="border border-accent/30 rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr ? 'Explorer le catalogue' : 'Explore the catalog'}</h2><Link className="inline-block mt-4 text-accent font-mono text-xs uppercase" href={`/${locale}/ecouteurs`}>{fr ? 'Voir le catalogue →' : 'Browse catalog →'}</Link></div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-5">FAQ</h2><div className="divide-y divide-line border-y border-line">{(fr ? [['Quels sont les meilleurs écouteurs pour voyager ?','Les modèles avec ANC, bonne autonomie et mode transparence peuvent être particulièrement pratiques pour les déplacements.'],['Quels écouteurs sont adaptés à l’avion ?','Les modèles équipés d’ANC peuvent aider à réduire certains bruits continus de cabine, selon leur efficacité réelle.'],['Quelle autonomie choisir pour voyager ?','Une autonomie plus longue réduit les recharges, mais l’autonomie annoncée peut varier selon l’usage.']] : [['What are the best earbuds for travel?','Models with ANC, good battery life and transparency mode can be especially practical for travel.'],['What earbuds are good for flights?','ANC models can help reduce some continuous cabin noise, depending on their real-world effectiveness.'],['How much battery life do you need for travel?','Longer battery life reduces charging, but advertised runtime can vary with usage.']]).map(([q,a]) => <details key={q} className="py-4"><summary className="cursor-pointer font-display font-medium">{q}</summary><p className="text-dim text-sm leading-7 mt-3 max-w-3xl">{a}</p></details>)}</div></section></article><Footer /></>;
}
