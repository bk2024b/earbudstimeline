import Link from 'next/link';
import { getAllEarbuds, getBrands } from '@/lib/queries';
import { canonicalFor, JsonLd } from '@/lib/seo';
import { Footer } from '@/components/UI';
import AdSlot from '@/components/AdSlot';

export const revalidate = 3600;
const n = (v) => Number.isFinite(Number(v)) ? Number(v) : null;
const price = (v) => n(v) == null ? '—' : `$${Math.round(n(v))}`;
const score = (m) => {
  let s = 45;
  const w = n(m.weight_g); if (w != null && w <= 7) s += 12;
  const b = n(m.battery_bud_h); if (b != null && b >= 6) s += 10;
  if (m.anc) s += 5;
  if (m.transparency_mode || m.transparency) s += 8;
  const ip = String(m.water_resistance ?? m.ip_rating ?? '').toUpperCase();
  if (/IPX[4-8]|IP[4-6][4-9]/.test(ip)) s += 20;
  return Math.min(100, s);
};
export async function generateMetadata({ params }) { const { locale } = params; const title = locale === 'fr' ? 'Meilleurs écouteurs pour la salle de sport en 2026' : 'Best Earbuds for the Gym in 2026'; return { title: `${title} | EarbudsTimeline`, description: locale === 'fr' ? 'Comparez les meilleurs écouteurs pour la salle de sport selon résistance à l’eau, poids, autonomie et fonctions disponibles.' : 'Compare the best earbuds for the gym using water resistance, weight, battery life and available features.', ...canonicalFor(`/${locale}/guides/best-earbuds-for-gym`) }; }
export default async function Page({ params }) {
  const { locale } = params; const fr = locale === 'fr'; const [models, brands] = await Promise.all([getAllEarbuds(), getBrands()]); const bm = new Map(brands.map(b => [b.id, b])); const rows = models.map(model => ({ model, score: score(model) })).sort((a,b) => b.score-a.score); const best = rows.slice(0,10); const budget = rows.filter(r => n(r.model.price) != null && n(r.model.price) <= 100).slice(0,5); const title = fr ? 'Meilleurs écouteurs pour la salle de sport en 2026' : 'Best Earbuds for the Gym in 2026'; const intro = fr ? 'Une sélection pour les séances en salle, basée sur les caractéristiques disponibles dans notre catalogue. Le score est un indicateur de spécifications et ne remplace pas un test de maintien ou de résistance en conditions réelles.' : 'A selection for gym sessions based on specifications available in our catalog. The score is a specification indicator and does not replace real-world fit or durability testing.'; const jsonLd = {'@context':'https://schema.org','@type':'Article',headline:title,description:intro,url:`https://earbudstimeline.com/${locale}/guides/best-earbuds-for-gym`,dateModified:'2026-08-26',inLanguage:locale}; const guide = slug => `/${locale}/guides/${slug}`;
  return <><JsonLd data={jsonLd}/><article className="max-w-6xl mx-auto"><div className="font-mono text-xs text-accent uppercase mb-3">Gym · 2026</div><h1 className="font-display font-bold text-[34px] sm:text-[48px] leading-tight mb-4">{title}</h1><p className="text-dim text-[15px] sm:text-[17px] leading-7 max-w-3xl">{intro}</p><div className="mt-4 text-[10px] font-mono text-dim">{fr ? 'Dernière mise à jour : 26 août 2026' : 'Last updated: August 26, 2026'}</div><section className="mt-8 grid grid-cols-2 gap-3"><a href="#overall" className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">OVERALL</div><div className="font-display font-semibold mt-1">{best.length} picks</div></a><a href="#budget" className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">UNDER $100</div><div className="font-display font-semibold mt-1">{budget.length} picks</div></a></section><section id="overall" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">🏋️ {fr ? 'Meilleurs écouteurs pour la salle de sport' : 'Best Earbuds for the Gym Overall'}</h2><div className="overflow-x-auto border border-line rounded-2xl bg-panel"><table className="w-full text-left text-xs"><thead className="border-b border-line font-mono text-[10px] text-dim uppercase"><tr><th className="p-4">#</th><th className="p-4">{fr ? 'Écouteurs' : 'Earbuds'}</th><th className="p-4">{fr ? 'Prix' : 'Price'}</th><th className="p-4">Score</th><th className="p-4">{fr ? 'Résistance' : 'Resistance'}</th></tr></thead><tbody>{best.map((r,i) => <tr key={r.model.id} className="border-b border-line last:border-0"><td className="p-4 text-accent">{i+1}</td><td className="p-4"><Link href={`/${locale}/ecouteurs/${r.model.id}`} className="font-semibold hover:text-accent">{r.model.name}</Link><div className="text-dim text-[10px]">{bm.get(r.model.brand_id)?.name || r.model.brand_id}</div></td><td className="p-4 font-mono">{price(r.model.price)}</td><td className="p-4 font-bold">{r.score}</td><td className="p-4">{r.model.water_resistance || r.model.ip_rating || '—'}</td></tr>)}</tbody></table></div></section><section id="budget" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">💵 {fr ? 'Meilleurs écouteurs de sport sous 100 $' : 'Best Gym Earbuds Under $100'}</h2><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{budget.map(r => <Link key={r.model.id} href={`/${locale}/ecouteurs/${r.model.id}`} className="bg-panel border border-line rounded-2xl p-5 hover:border-accent"><div className="font-mono text-[10px] text-accent">{bm.get(r.model.brand_id)?.name || r.model.brand_id}</div><h3 className="font-display font-semibold mt-1">{r.model.name}</h3><div className="mt-3 font-display font-bold">{price(r.model.price)}</div><div className="mt-3 text-[10px] font-mono text-dim">{r.model.water_resistance || r.model.ip_rating || 'Resistance data —'} · {n(r.model.weight_g) != null ? `${n(r.model.weight_g)}g` : 'Weight —'}</div></Link>)}</div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Résistance à la transpiration et à l’eau' : 'Sweat and water resistance'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Les séances en salle peuvent exposer les écouteurs à la transpiration et à l’humidité. Une certification IP peut fournir une indication utile, mais elle ne signifie pas que les écouteurs sont invulnérables à tous les liquides ou à toutes les conditions.' : 'Gym sessions can expose earbuds to sweat and moisture. An IP rating can provide a useful indication, but it does not mean earbuds are invulnerable to every liquid or condition.'}</p></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Maintien et confort pendant les exercices' : 'Fit and comfort during workouts'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Le maintien réel dépend de la forme de l’oreille, des embouts et du mouvement. Le poids peut aider à comparer les modèles, mais les données de poids ne suffisent pas à prédire le confort pendant une séance.' : 'Real-world fit depends on ear shape, tips and movement. Weight can help compare models, but weight data alone cannot predict workout comfort.'}</p></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-4">{fr ? 'Méthodologie' : 'Methodology'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr ? 'Le score Gym combine résistance à l’eau lorsqu’elle est disponible, poids, autonomie, transparence et ANC. Les données manquantes ne sont pas inventées et le score ne mesure pas le maintien réel pendant l’exercice.' : 'The Gym score combines available water resistance, weight, battery life, transparency and ANC data. Missing data is not invented, and the score does not measure real-world fit during exercise.'}</p></section><section className="mt-14 grid md:grid-cols-2 gap-4"><div className="border border-line rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr ? 'Guides associés' : 'Related guides'}</h2><div className="mt-4 space-y-2 text-sm"><Link className="block hover:text-accent" href={guide('best-earbuds-for-running')}>→ Best Earbuds for Running</Link><Link className="block hover:text-accent" href={guide('best-earbuds-for-sport')}>→ Best Earbuds for Sport</Link><Link className="block hover:text-accent" href={guide('best-earbuds-for-working')}>→ Best Earbuds for Working</Link></div></div><div className="border border-accent/30 rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr ? 'Explorer le catalogue' : 'Explore the catalog'}</h2><Link className="inline-block mt-4 text-accent font-mono text-xs uppercase" href={`/${locale}/ecouteurs`}>{fr ? 'Voir le catalogue →' : 'Browse catalog →'}</Link></div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-5">FAQ</h2><div className="divide-y divide-line border-y border-line">{(fr ? [['Quels sont les meilleurs écouteurs pour la salle de sport ?','Privilégiez notamment un maintien stable, une résistance adaptée à la transpiration, un poids raisonnable et une autonomie suffisante.'],['Les écouteurs doivent-ils avoir une certification IP pour la salle ?','Une certification IP peut être un indicateur utile de résistance, mais le niveau pertinent dépend de votre usage.'],['Les écouteurs ANC sont-ils utiles à la salle ?','Ils peuvent réduire certaines distractions, mais le mode transparence peut être préférable lorsque vous devez rester attentif à votre environnement.']] : [['What are the best earbuds for the gym?','Look for secure fit, suitable sweat resistance, reasonable weight and enough battery life.'],['Do gym earbuds need an IP rating?','An IP rating can be a useful resistance indicator, but the appropriate level depends on your use.'],['Is ANC useful at the gym?','It can reduce some distractions, but transparency mode may be preferable when you need environmental awareness.']]).map(([q,a]) => <details key={q} className="py-4"><summary className="cursor-pointer font-display font-medium">{q}</summary><p className="text-dim text-sm leading-7 mt-3 max-w-3xl">{a}</p></details>)}</div></section></article><AdSlot variant="native" zoneKey={process.env.NEXT_PUBLIC_ADSTERRA_SITEWIDE_NATIVE_KEY} invokeDomain={process.env.NEXT_PUBLIC_ADSTERRA_SITEWIDE_NATIVE_DOMAIN} label={locale === 'en' ? 'Advertisement' : 'Publicité'} /><Footer /></>;
}
