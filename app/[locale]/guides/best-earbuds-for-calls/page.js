import Link from 'next/link';
import { getAllEarbuds, getBrands } from '@/lib/queries';
import { canonicalFor, JsonLd } from '@/lib/seo';
import { Footer } from '@/components/UI';

export const revalidate = 3600;
const n = (v) => Number.isFinite(Number(v)) ? Number(v) : null;
const formatPrice = (v) => n(v) == null ? '—' : `$${Math.round(n(v))}`;
function micScore(model) { const count = [model.microphones, model.mic_count].map(n).find(v => v != null); return count == null ? null : Math.min(100, 55 + count * 15); }
function Card({ row, brand, fr, locale }) { const { model, score } = row; return <Link href={`/${locale}/ecouteurs/${model.id}`} className="block bg-panel border border-line rounded-2xl p-5 hover:border-accent transition-colors"><div className="flex justify-between gap-4"><div className="min-w-0"><div className="font-mono text-[10px] text-accent uppercase">{brand?.name || model.brand_id}</div><h3 className="font-display font-semibold text-[17px] mt-1">{model.name}</h3></div><div className="text-right shrink-0"><div className="font-display font-bold text-xl">{formatPrice(model.price)}</div><div className="font-mono text-[9px] text-accent mt-1">{fr ? 'Appels' : 'Calls'} {score}/100</div></div></div><div className="mt-4 grid grid-cols-2 gap-2 text-[10px] font-mono"><div className="border border-line rounded-lg p-2"><span className="text-dim">{fr ? 'Micros' : 'Mics'}</span><strong className="block text-sm mt-1">{n(model.microphones) ?? n(model.mic_count) ?? '—'}</strong></div><div className="border border-line rounded-lg p-2"><span className="text-dim">ANC</span><strong className="block text-sm mt-1">{model.anc ? 'Yes' : 'No'}</strong></div></div></Link>; }
export async function generateMetadata({ params }) { const { locale } = params; const title = locale === 'fr' ? 'Meilleurs écouteurs pour les appels en 2026' : 'Best Earbuds for Calls in 2026'; return { title: `${title} | EarbudsTimeline`, description: locale === 'fr' ? 'Comparez les meilleurs écouteurs pour les appels selon les données microphone disponibles.' : 'Compare the best earbuds for calls using available microphone data.', ...canonicalFor(`/${locale}/guides/best-earbuds-for-calls`) }; }
export default async function Page({ params }) { const { locale } = params; const fr = locale === 'fr'; const [models, brands] = await Promise.all([getAllEarbuds(), getBrands()]); const brandMap = new Map(brands.map(b => [b.id, b])); const rows = models.map(model => ({ model, score: micScore(model) })).filter(r => r.score != null).sort((a,b) => b.score-a.score); const best = rows.slice(0,10); const anc = rows.filter(r => r.model.anc).slice(0,5); const under100 = rows.filter(r => n(r.model.price) != null && n(r.model.price) <= 100).slice(0,5); const battery = [...rows].filter(r => n(r.model.battery_bud_h) != null).sort((a,b) => n(b.model.battery_bud_h)-n(a.model.battery_bud_h)).slice(0,5); const title = fr ? 'Meilleurs écouteurs pour les appels en 2026' : 'Best Earbuds for Calls in 2026'; const intro = fr ? 'Une sélection basée sur les données microphone disponibles dans notre catalogue. Le score appels est un indice de spécifications et ne remplace pas un test réel de qualité vocale.' : 'A selection based on microphone data available in our catalog. The calls score is a specification index and does not replace real-world voice testing.'; const jsonLd = { '@context':'https://schema.org','@type':'Article',headline:title,description:intro,url:`https://earbudstimeline.com/${locale}/guides/best-earbuds-for-calls`,dateModified:'2026-08-25',inLanguage:locale }; const section = (id, heading, items, subtitle) => items.length > 0 && <section id={id} className="mt-12"><div className="mb-5"><h2 className="font-display font-semibold text-[25px]">{heading}</h2><p className="text-dim text-sm mt-1">{subtitle}</p></div><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{items.map(r => <Card key={r.model.id} row={r} brand={brandMap.get(r.model.brand_id)} fr={fr} locale={locale}/>)}</div></section>; return <><JsonLd data={jsonLd}/><article className="max-w-6xl mx-auto"><div className="font-mono text-xs text-accent uppercase mb-3">Calls · 2026</div><h1 className="font-display font-bold text-[34px] sm:text-[48px] leading-tight mb-4">{title}</h1><p className="text-dim text-[15px] sm:text-[17px] leading-7 max-w-3xl">{intro}</p><div className="mt-4 text-[10px] font-mono text-dim">{fr ? 'Dernière mise à jour : 25 août 2026' : 'Last updated: August 25, 2026'}</div><section className="mt-8 grid grid-cols-2 lg:grid-cols-4 gap-3">{[['overall','OVERALL',best],['anc','ANC',anc],['under100','UNDER $100',under100],['battery','BATTERY',battery]].map(([id,label,items])=><a key={id} href={`#${id}`} className="bg-panel border border-line rounded-xl p-4 hover:border-accent"><div className="font-mono text-accent text-xs">{label}</div><div className="font-display font-semibold mt-1">{items.length} picks</div></a>)}</section><section id="overall" className="mt-12"><h2 className="font-display font-semibold text-[25px] mb-5">📞 {fr?'Meilleurs écouteurs pour les appels':'Best Earbuds for Calls Overall'}</h2><div className="overflow-x-auto border border-line rounded-2xl bg-panel"><table className="w-full text-left text-xs"><thead className="border-b border-line font-mono text-[10px] text-dim uppercase"><tr><th className="p-4">#</th><th className="p-4">{fr?'Écouteurs':'Earbuds'}</th><th className="p-4">{fr?'Prix':'Price'}</th><th className="p-4">Calls</th><th className="p-4">Mic</th><th className="p-4">ANC</th></tr></thead><tbody>{best.map((r,i)=><tr key={r.model.id} className="border-b border-line last:border-0"><td className="p-4 font-mono text-accent">{i+1}</td><td className="p-4"><Link className="font-semibold hover:text-accent" href={`/${locale}/ecouteurs/${r.model.id}`}>{r.model.name}</Link><div className="text-dim text-[10px] mt-1">{brandMap.get(r.model.brand_id)?.name || r.model.brand_id}</div></td><td className="p-4 font-mono">{formatPrice(r.model.price)}</td><td className="p-4 font-mono font-bold">{r.score}</td><td className="p-4">{n(r.model.microphones)??n(r.model.mic_count)??'—'}</td><td className="p-4">{r.model.anc?'✓':'—'}</td></tr>)}</tbody></table></div></section>{section('anc',fr?'🎙️ Meilleurs écouteurs ANC pour les appels':'🎙️ Best ANC Earbuds for Calls',anc,fr?'Sélection parmi les modèles avec ANC.':'Selection among ANC models.')}{section('under100',fr?'💵 Meilleurs écouteurs pour les appels sous 100 $':'💵 Best Earbuds for Calls Under $100',under100,fr?'Sélection sous 100 $.':'Under-$100 selection.')}{section('battery',fr?'🔋 Meilleure autonomie pour les appels':'🔋 Best Battery Life for Calls',battery,fr?'Autonomie annoncée par écouteur.':'Advertised per-earbud battery life.')}<section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-5">{fr?'Méthodologie':'Methodology'}</h2><p className="text-dim text-sm leading-7 max-w-3xl">{fr?'Le score Calls est un indice simple dérivé du nombre de microphones lorsque cette donnée existe. Il ne constitue pas une mesure de clarté vocale ou de performance dans un environnement réel.':'The Calls score is a simple index derived from microphone count when that data exists. It is not a measurement of voice clarity or real-world performance.'}</p></section><section className="mt-14 grid md:grid-cols-2 gap-4"><div className="border border-line rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr?'Guides associés':'Related guides'}</h2><div className="mt-4 space-y-2 text-sm"><Link className="block hover:text-accent" href={`/${locale}/guides/best-wireless-earbuds`}>→ Best Wireless Earbuds</Link><Link className="block hover:text-accent" href={`/${locale}/guides/best-noise-cancelling-earbuds`}>→ Best Noise Cancelling Earbuds</Link><Link className="block hover:text-accent" href={`/${locale}/guides/best-earbuds-under-100`}>→ Best Earbuds Under $100</Link></div></div><div className="border border-accent/30 rounded-2xl p-6 bg-panel"><h2 className="font-display font-semibold text-xl">{fr?'Explorer le catalogue':'Explore the catalog'}</h2><Link className="inline-block mt-4 text-accent font-mono text-xs uppercase" href={`/${locale}/ecouteurs`}>{fr?'Voir le catalogue →':'Browse catalog →'}</Link></div></section><section className="mt-14"><h2 className="font-display font-semibold text-[25px] mb-5">FAQ</h2><div className="divide-y divide-line border-y border-line">{(fr?[['Quels sont les meilleurs écouteurs pour les appels ?','Notre classement compare les modèles pour lesquels une donnée microphone exploitable est disponible.'],['Le nombre de microphones garantit-il une meilleure qualité ?','Non. C’est seulement un indicateur de spécifications.'],['Quels écouteurs pour les appels sous 100 $ ?','Utilisez la section dédiée sous 100 $ pour voir les modèles correspondants.'],['Le classement mesure-t-il la voix en environnement bruyant ?','Non. Il ne remplace pas des tests d’appel dédiés.']]:[['What are the best earbuds for calls?','Our ranking compares models for which usable microphone data is available.'],['Does more microphones guarantee better quality?','No. It is only a specification indicator.'],['What are the best earbuds for calls under $100?','Use the dedicated under-$100 section to see matching models.'],['Does the ranking measure voice quality in noisy environments?','No. It does not replace dedicated call tests.']]).map(([q,a])=><details key={q} className="py-4"><summary className="cursor-pointer font-display font-medium">{q}</summary><p className="text-dim text-sm leading-7 mt-3 max-w-3xl">{a}</p></details>)}</div></section></article><Footer/></>;
}
